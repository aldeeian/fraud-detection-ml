--extra-index-url https://download.pytorch.org/whl/cpu
# Core data science libraries
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0

# Machine learning
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0

# Deep learning (PyTorch - CPU version, works on all Windows machines)
# If you have an NVIDIA GPU, replace with: pip install torch --index-url https://download.pytorch.org/whl/cu121
# torch is OPTIONAL. The LSTM is skipped if torch is missing; everything else works.
# CPU install: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0

# Dashboard and visualization
streamlit>=1.28.0
plotly>=5.17.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
joblib>=1.3.0
